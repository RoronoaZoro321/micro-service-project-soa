apiVersion: apps/v1
kind: Deployment
metadata:
    name: transaction-mongo-depl
spec:
    replicas: 1
    selector:
        matchLabels:
            app: transaction-mongo
    template:
        metadata:
            labels:
                app: transaction-mongo
        spec:
            containers:
                - name: transaction-mongo
                  image: mongo
                  volumeMounts:
                      - name: mongo-data
                        mountPath: /data/db # Mounting the volume at MongoDB's data directory
            volumes:
                - name: mongo-data
                  persistentVolumeClaim:
                      claimName: transaction-mongo-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: transaction-mongo-pvc
spec:
    accessModes:
        - ReadWriteOnce # Allows the volume to be mounted by a single node
    resources:
        requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
    name: transaction-mongo-srv
spec:
    selector:
        app: transaction-mongo
    ports:
        - name: db
          protocol: TCP
          port: 27017
          targetPort: 27017
