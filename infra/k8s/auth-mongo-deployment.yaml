apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: auth-mongo
  name: auth-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: auth-mongo
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: auth-mongo
    spec:
      containers:
        - image: mongo
          name: auth-mongo
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
