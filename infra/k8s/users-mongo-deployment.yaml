apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: users-mongo
  name: users-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: users-mongo
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: users-mongo
    spec:
      containers:
        - image: mongo
          name: users-mongo
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
